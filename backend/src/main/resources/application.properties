spring.application.name=gym-exercise-tracker

# MongoDB URI — uses MONGODB_URI if set (Render), otherwise falls back to Atlas connection locally
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://niteshpokhrel84_db_user:AbO57gnDNmXPk828@cluster0.gbyqe3x.mongodb.net/gymtracker?retryWrites=true&w=majority&appName=Cluster0}

# Auto create indexes (for unique usernames, emails, etc.)
spring.data.mongodb.auto-index-creation=true

# Server port — Render/Railway inject $PORT dynamically; fallback to 8082 locally
server.port=${PORT:8082}

# JWT — secret must be set via JWT_SECRET environment variable on deployment
jwt.secret=${JWT_SECRET:mySuperSecretKeyForDevelopmentTestingOnlyMustBeLongEnough123!}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# CORS — set via env var on deployment; defaults allow local dev
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# Security debug logging (disable in production)
logging.level.org.springframework.security=WARN