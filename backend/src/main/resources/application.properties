spring.application.name=gym-exercise-tracker

# Correct Spring Data MongoDB URI with environment fallback
# Notice the database name is placed right before the '?'
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://niteshpokhrel84_db_user:AbO57gnDNmXPk828@cluster0.gbyqe3x.mongodb.net/gym_exercise_tracker?retryWrites=true&w=majority&appName=Cluster0}

# Auto create indexes (for unique usernames, emails, etc.)
spring.data.mongodb.auto-index-creation=true

# Server port (Render/Railway dynamically inject $PORT)
server.port=${PORT:8080}

# JWT Configurations
jwt.secret=${JWT_SECRET:mySuperSecretKeyForGymTrackerAppMustBeLongEnough123!}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
